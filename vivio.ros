#!/bin/sh
#|-*- mode:lisp -*-|#
#| <Put a one-line description here>
exec ros -Q -- $0 "$@"
|#

(require :cl-cffi-gtk)

(defpackage :vivio
  (:use :common-lisp :cl-user :gtk))

(in-package :vivio)

(defun main (&rest argv)
  (declare (ignorable argv)))
